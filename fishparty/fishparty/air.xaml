<Page
    x:Class="fishparty.air"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fishparty"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:fishparty"
    mc:Ignorable="d">

    <Page.Resources>
        <ImageBrush x:Name="background" ImageSource="Assets/air_background.jpg" Opacity=".5"/>
    </Page.Resources>

    <Grid Background="{StaticResource background}">

        <Grid Style="{StaticResource title_grid_style}">
            <Rectangle Style="{StaticResource title_box_style}" Width="889"/>
            <TextBlock Style="{StaticResource title_text_style}" >Air Collection</TextBlock>
        </Grid>

        <Button x:Name="button" Style="{StaticResource guided_forward_button}" Click="nav_to_fish_release" FontSize="24">Fish Release -></Button>

        <Grid x:Name="contentPanel" VerticalAlignment="Bottom" HorizontalAlignment="Left">
            <Button Content="Jump to Section" FontSize="20">
                <Button.Flyout>
                    <controls:guided_tour_nav />
                </Button.Flyout>
            </Button>
        </Grid>
    </Grid>
</Page>
