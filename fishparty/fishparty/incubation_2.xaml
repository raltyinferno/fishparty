<Page
    x:Class="fishparty.incubation_2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fishparty"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:fishparty"
    mc:Ignorable="d">

    <Page.Resources>
        <ImageBrush ImageSource="Assets/Egg Room Guided Tour/egg_002.jpg" x:Key="walk1" Opacity="1" Stretch="UniformToFill"/>
    </Page.Resources>

    <Grid Background="{StaticResource walk1}" x:Name="page_grid">
        <Grid Style="{StaticResource title_grid_style}">
            <Rectangle Style="{StaticResource title_box_style}" Width="669" Margin="0,0,-29,0"/>
            <TextBlock Style="{StaticResource title_text_style}">Incubation</TextBlock>
        </Grid>
        


        <Grid  VerticalAlignment="Bottom" HorizontalAlignment="Left">
            <Button Content="Jump to Section" FontSize="20">
                <Button.Flyout>
                    <controls:guided_tour_nav/>
                </Button.Flyout>
            </Button>
        </Grid>

        <local:home_button/>
        <Button x:Name="egg_butt" Content="5ft Tanks ->" Style="{StaticResource guided_forward_button}" Click="next_page_Click"/>
    </Grid>
</Page>
